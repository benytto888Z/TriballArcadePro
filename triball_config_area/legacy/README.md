# legacy/ — copies obsolètes (non compilées)

Anciennes versions hors de `lib/` : ni compilées, ni analysées. Imports réécrits en
`package:amzneontriballui/...` pour rester lisibles.

| Fichier | Remplacé par |
|---|---|
| `widgets/futuristic_config_area_lock_overlay_old.dart` | overlay de verrouillage courant de la Config Area |
